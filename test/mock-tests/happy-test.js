/* eslint-env mocha */
/* eslint-disable no-unused-expressions */
const { expect } = require('chai');

describe('Everything is awesome', () => {
  it('everything is cool when you\'re part of a team', () => {
    expect(true).to.be.true;
  });
});
